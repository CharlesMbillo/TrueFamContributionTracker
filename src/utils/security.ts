// src/utils/security.ts
export const initSecureStorage = async (): Promise<void> => {
  console.log('Secure storage initialized');
};

export const getSecuredValue = async (key: string): Promise<string | null> => {
  return null;
};